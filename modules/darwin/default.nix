{ config, pkgs, ... }:

{
  # macOS-specific configuration (placeholder for future use)
  # This module is for Darwin (macOS) specific settings
  # Currently empty as this is a NixOS-only repository
}